export function ExpressionSqrtMemberView() {
  // const [start, end] = member.choices;
  // console.log(member.submembers)
  // return <Box sx={{position: 'relative'}}>
  //   <Box>
  //     <Latex large mathExpr={`
  //       ${start.viewSymbol}
  //       |${(member.submembers.map(x => x.choices.length > 1 ? '\\hspace{0.879em}|' : '\\hspace{0.325em}|').join(''))}
  //       ${end.viewSymbol}
  //     `} />
  //   </Box>
  //   <Box sx={{position: 'absolute', top: 10, left: '70.75px', gap: 1, display: 'flex'}}>
  //     {member.submembers.map(x => <React.Fragment key={x.id}>{x.renderJSX()}</React.Fragment>)}
  //   </Box>
  // </Box>
  return <></>;
}

